import React from "react";

const JsxRules = () => {
  return (
    <div>
      <h1>JSX Rules:</h1>
      <p>JSX must return a single parent element.</p>
      <p>JSX elements must be properly closed.</p>
      <p>
        JSX attributes are written using camelCase(e.g., "className" insted of
        "class")
      </p>
    </div>
  );
};

export default JsxRules;
